name = "card-max-scraper-poc"
main = "scraper.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Optional — only needed for render:true (Sampath SPA API). Requires the
# Workers Paid plan ($5/mo minimum) or the free-tier 10 min/day allowance.
# Uncomment once Browser Rendering is enabled on the account:
# [browser]
# binding = "BROWSER"

# WORKER_SECRET is set via `wrangler secret put WORKER_SECRET`, not here.
